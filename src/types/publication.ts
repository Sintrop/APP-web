export interface PublicationProps {
    id: string;
    userId: string;
    description: string;
    images: string;
    type: string;
    origin: string;
    additionalData: string;
    createdAt: string;
}